# pytorch

- [LINEAR](https://pytorch.org/docs/stable/generated/torch.nn.Linear.html)

- ```
  m = nn.Linear(20, 30)
  input = torch.randn(128, 20)
  output = m(input)
  print(output.size())
  ```

- 